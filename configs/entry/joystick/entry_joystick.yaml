# PlanetJoystick process entry; pass this file to planetj, not the Cadence runtime.
# Change the device or destination in the referenced producer configuration.
extends: ../../operators/joystick.yaml
